import React from "react";
import { Typography } from "@mui/material";

const AdminDashboard = () => (
  <Typography variant="h4">Admin Dashboard (Hospitals, Labs, Pharmacies)</Typography>
);

export default AdminDashboard;
