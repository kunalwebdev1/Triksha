import React from "react";
import { Typography } from "@mui/material";

const HospitalDashboard = () => (
  <Typography variant="h4">Hospital Dashboard (Inventory, OPD)</Typography>
);

export default HospitalDashboard;
