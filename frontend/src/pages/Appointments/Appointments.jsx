import React from "react";
import { Typography } from "@mui/material";

const Appointments = () => (
  <Typography variant="h5">Appointments & Queue Management</Typography>
);

export default Appointments;
