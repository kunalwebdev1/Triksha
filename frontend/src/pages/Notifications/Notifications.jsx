import React from "react";
import { Typography } from "@mui/material";

const Notifications = () => (
  <Typography variant="h5">Smart Health Notifications & Alerts</Typography>
);

export default Notifications;
